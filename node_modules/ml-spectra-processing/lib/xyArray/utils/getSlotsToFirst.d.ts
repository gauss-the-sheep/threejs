import type { DataXY } from 'cheminfo-types';
export interface GetSlotsToFirstOptions {
    /**
     * The range in which the two x values of the data/spectra must be to be placed on the same line. It may also be a function that allows to change `delta` depending on the X values of the spectrum
     * @default 1
     */
    delta?: ((arg: number) => number) | number;
}
export interface Slot {
    from: number;
    to: number;
    value: number;
}
/**
 * Computes slots aligned to the first spectrum's x values, adding slots for any x values not covered.
 * @param data - data.
 * @param options - options.
 * @returns the slots, sorted by x value, with non-overlapping `[from, to)` ranges.
 */
export declare function getSlotsToFirst(data: DataXY[], options?: GetSlotsToFirstOptions): Slot[];
//# sourceMappingURL=getSlotsToFirst.d.ts.map